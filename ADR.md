# Architecture Decision Records

ดูศัพท์ที่ใช้ในเอกสารนี้ได้ที่ [CONTEXT.md](CONTEXT.md)

---

## ADR-001: เพิ่มโหมดร่างเอกสาร + Auto-scrutinize แยกจาก Q&A เดิม

**สถานะ**: Implemented แล้ว (ผ่าน `test_rag_pipeline.py` ครบ 4 เทส) — ดู ADR-002 สำหรับการต่อยอด
**วันที่**: 2026-07-02

### บริบท (Context)

ระบบเดิมออกแบบมาเป็น retrieval Q&A ล้วนๆ — system prompt บังคับ grounding แบบเข้มงวด (ห้ามแต่งคำตอบเองนอกเหนือ context) เพื่อป้องกัน hallucination ทดสอบแล้วพบว่าถ้าขอให้ระบบ "เขียนนโยบายจัดซื้อใหม่ทั้งฉบับ" ระบบจะปฏิเสธหรือตอบว่าไม่พบข้อมูล เพราะไม่มีเอกสารจัดซื้อในฐานข้อมูลให้ค้น — พฤติกรรมนี้ถูกต้องตามที่ออกแบบไว้ ไม่ใช่บั๊ก แต่หมายความว่าระบบไม่มีความสามารถ "ร่างเอกสารใหม่" เลย

ผู้ใช้ต้องการให้ระบบช่วยร่างนโยบายใหม่ (เช่น นโยบายจัดซื้อ) โดยอ้างอิงนโยบายที่มีอยู่แล้ว (IT Policy, Risk Management) ให้สอดคล้องกัน และต้องการให้มีการตรวจสอบความสอดคล้อง/ความขัดแย้งก่อนนำไปใช้จริง

โมเดลที่ใช้ปัจจุบัน (`gemini-3.1-flash-lite`) มี FACTS (ความแม่นยำเชิงข้อเท็จจริง) แค่ 40.6% และไม่มี benchmark กลุ่ม agentic/expert-task เพราะออกแบบมาสำหรับงานเบา ไม่เหมาะกับงานร่าง+วิจารณ์เอกสารที่ต้องการ reasoning หลายขั้นตอน เทียบกับ `gemini-3.5-flash` ซึ่งในงานประเภทใกล้เคียง (Finance Agent v2 — วิเคราะห์เอกสารเชิงลึก, MCP Atlas — multi-step workflow) ทำคะแนนได้ดีกว่า `gemini-3.1-pro` ด้วยซ้ำ ในราคาที่ถูกกว่า Pro ครึ่งหนึ่ง และยังอยู่ใน free tier (Pro ถูกถอดออกจาก free tier ตั้งแต่เม.ย. 2026)

### การตัดสินใจ (Decision)

1. **เพิ่มโหมดใหม่แยกจาก Q&A เดิมโดยสิ้นเชิง** — ไม่แก้ system prompt เดิม ไม่กระทบพฤติกรรม Q&A ที่ทดสอบแล้วว่าทำงานถูกต้อง เปิดผ่านปุ่ม/toggle แยกชัดเจนในหน้าเว็บ (ไม่ใช้ slash command ในกล่องแชทเดิม เพื่อป้องกันผู้ใช้เผลอสั่งโหมดผิดจากการพิมพ์ปกติ)

2. **Draft → Scrutinize เป็น flow เดียวกัน ไม่ใช่ 2 ปุ่มอิสระ** — กด "ร่างนโยบาย" ครั้งเดียว ระบบร่างแล้ววิจารณ์ตัวเองทันทีในคำขอเดียว ผู้ใช้ไม่ต้องกด 2 รอบ ได้ทั้งร่าง + รายงานจุดที่ต้องระวังพร้อมกัน

3. **ใช้ `gemini-3.5-flash` แยกจากโมเดล Q&A เดิม** ผ่าน env var ใหม่ `GEMINI_MODEL_DRAFT` (ค่าเริ่มต้น `gemini-3.5-flash`) ส่วน Q&A เดิมย้ายจาก hardcode เป็น `GEMINI_MODEL_CHAT` (ค่าเริ่มต้น `gemini-3.1-flash-lite`) เพื่อให้ปรับ model ได้จาก `.env` โดยไม่ต้องแก้โค้ด สอดคล้องกับแนวทาง `COMPANY_NAME` ที่ทำไว้แล้ว

4. **Scrutinize ใช้ระดับความรุนแรง CRITICAL/WARNING/NITPICK/VERDICT** (มาตรฐานเดียวกับ `/scrutinize` skill) — ถ้าเจอ CRITICAL **ไม่บล็อกการดาวน์โหลด** แค่โชว์คำเตือนเด่นชัด เพราะร่างเป็นแค่จุดตั้งต้นสำหรับมนุษย์ไปแก้ต่อ ไม่ใช่เอกสารสำเร็จรูป การบล็อกจะขัดขวาง workflow โดยไม่จำเป็น

5. **ผลลัพธ์ v1 เป็น markdown ในแชท + ดาวน์โหลด .docx โครงสร้างเนื้อหาเปล่า** (หัวข้อ/ย่อหน้า/bullet) **ไม่เลียนแบบ template บริษัทเต็มรูปแบบ** (ไม่มีตารางเซ็นสั่ง/ทวนสอบ, ประวัติแก้ไข, หัวกระดาษ) — ลดความซับซ้อนของ v1 ผู้ใช้คัดลอกเนื้อหาไปใส่ template จริงเองก่อนเสนออนุมัติ

6. **ยังคงใช้ FAISS index + reranker ชุดเดียวกับ Q&A** สำหรับดึง context มาให้ทั้งขั้นตอนร่างและ scrutinize ไม่สร้าง index แยก

### ผลที่ตามมา (Consequences)

- ต้นทุนต่อการเรียกโหมดร่างสูงกว่า Q&A ปกติ (2 ครั้งเรียก LLM ต่อ 1 คำขอ: ร่าง + scrutinize, ราคาโมเดลแพงกว่าด้วย) แต่ความถี่การใช้งานต่ำ (ไม่บ่อยเท่าถามคำถาม) ผลกระทบต้นทุนรวมจึงต่ำ
- ต้อง maintain 2 system prompt แยกกัน (Q&A แบบเข้มงวด vs draft แบบผ่อนคลาย) — ความเสี่ยงคือถ้าดูแลไม่ดีอาจสับสนว่า prompt ไหนใช้กับ endpoint ไหน ต้องตั้งชื่อฟังก์ชันให้ชัดเจน (`_build_qa_sys_prompt` vs `_build_draft_sys_prompt` vs `_build_scrutinize_sys_prompt`)
- ร่างที่ได้ไม่ตรง template บริษัทจริง (ตาม decision ข้อ 5) — ถ้าผู้ใช้ต้องการ fidelity สูงขึ้นในอนาคต ต้องทำ ADR ใหม่แยกเพื่อประเมิน effort การทำ docx-js/python-docx ที่ตรง template จริง (ตาราง, header, logo)
- ผลลัพธ์จาก draft mode "ไม่ได้ผ่านการ grounding อย่างเข้มงวด" เหมือน Q&A เดิม — ต้องติดป้ายกำกับในหน้าเว็บและในตัวเอกสารว่าเป็น "ร่างที่สร้างโดย AI ต้องมีมนุษย์ตรวจสอบและอนุมัติก่อนใช้จริง" เสมอ เพื่อป้องกันการเข้าใจผิดว่าเป็นนโยบายที่ผ่านการอนุมัติแล้ว

---

## ADR-002: เพิ่มขั้นตอนถามคำถามเพิ่มเติม (Clarifying Questions) ก่อนร่างเอกสาร

**สถานะ**: อนุมัติแล้ว (ยังไม่ implement)
**วันที่**: 2026-07-02

### บริบท (Context)

หลัง implement ADR-001 เสร็จและทดสอบจริง ผู้ใช้ทดลองโหมดร่างเอกสารด้วยหัวข้อ "นโยบาย PDPA" แล้วสังเกตว่าฟอร์มมีแค่ช่อง "หัวข้อ" กับ "คำสั่งเพิ่มเติม" แล้วกดร่างได้เลยทันที โดยระบบไม่เคยถามข้อมูลอะไรกลับมาก่อน — ทั้งที่นโยบายบางประเภท (เช่น PDPA) ต้องพึ่งข้อเท็จจริงเฉพาะองค์กรที่ **ไม่มีอยู่ใน context ที่ retrieve ได้เลย** (เช่น เก็บข้อมูลส่วนบุคคลประเภทไหนบ้าง, มี DPO หรือไม่/เป็นใคร, ระยะเวลาเก็บรักษาข้อมูล, ฐานทางกฎหมายที่ใช้) เพราะ corpus ที่มีอยู่ตอนนี้ (IT Policy, Risk Management) ไม่ครอบคลุมเรื่องนี้

นี่คือความเสี่ยงที่ ADR-001 ข้อ 4/consequences เคยเตือนไว้แล้วในเชิงหลักการ ("ร่างที่ได้ไม่ได้ผ่าน grounding เข้มงวด") แต่ยังไม่มีกลไกที่ **ป้องกันเชิงรุก** ไม่ให้ AI ต้องเดา/มั่วรายละเอียดที่มันไม่มีทางรู้ได้จริง — scrutinize ที่มีอยู่เดิมเป็นการตรวจ "หลังร่างเสร็จ" เท่านั้น ไม่ได้ช่วยป้องกันไม่ให้ AI แต่งรายละเอียดที่ไม่มีมูลตั้งแต่แรก

ตัดสินใจผ่านเซสชัน grill-with-docs กับผู้ใช้ (ทุกข้อเลือกตัวเลือกที่แนะนำ):

### การตัดสินใจ (Decision)

1. **เพิ่มขั้นตอนใหม่ก่อนร่าง: AI สร้างคำถามเฉพาะหัวข้อแบบไดนามิก** — ไม่ใช้ checklist ตายตัว เพราะแต่ละหัวข้อนโยบายต้องการข้อมูลต่างกันโดยสิ้นเชิง (PDPA ต้องถามเรื่องข้อมูลส่วนบุคคล, นโยบายจัดซื้ออาจถามเรื่องวงเงินอนุมัติ) checklist ตายตัวจะครอบคลุมไม่พอหรือถามคำถามที่ไม่เกี่ยวข้อง

2. **คำถามต้องอิงจาก context ที่ retrieve ได้ก่อน แล้วถามเฉพาะ "จุดที่ context ไม่ครอบคลุม"** — ไม่ใช่ถามจาก topic ลอยๆ เพราะเป้าหมายคืออุดช่องโหว่ตรงจุดที่ AI จะต้องมั่วจริงๆ (ถ้า context มีข้อมูลอยู่แล้วไม่ต้องถามซ้ำ)

3. **ถาม-ตอบรอบเดียวแบบฟอร์ม ไม่ใช่ chat หลายรอบ** — สร้างคำถาม 3-6 ข้อครั้งเดียว ให้ผู้ใช้กรอกทีเดียวแล้วกดยืนยัน คงความเรียบง่ายของ workflow ไว้ตามแนวทาง ADR-001 (ไม่อยากให้กลายเป็นบทสนทนา back-and-forth ยาว)

4. **ผู้ใช้ข้ามคำถามที่ตอบไม่ได้ได้ ไม่บังคับตอบครบ** — สอดคล้องกับหลักการ "ไม่บล็อก แค่เตือน" ที่ ADR-001 ข้อ 4 วางไว้แล้ว (CRITICAL gate) คำถามที่ถูกข้ามจะถูก AI มาร์กเป็น `[ต้องระบุ: <คำถามนั้น>]` ตรงจุดที่เกี่ยวข้องในร่าง แทนที่จะเดาคำตอบขึ้นมาเอง และ scrutinize จะยกจุดที่ยังมีเครื่องหมายนี้ค้างอยู่ขึ้นมาเป็น WARNING เสมอ

5. **UI เป็น 2 ปุ่มแยกขั้นตอนชัดเจน**: "❓ สร้างคำถามเพิ่มเติม" (เรียก `/draft/questions`) → แสดงฟอร์มคำถามให้กรอก → "✅ ยืนยันร่างนโยบาย" (เรียก `/draft` พร้อม answers) — ไม่ใช้ปุ่มเดียวสองสถานะ เพื่อไม่ให้ผู้ใช้สับสนว่ากดแล้วจะเกิดอะไรขึ้น

6. **ขั้นตอนสร้างคำถามใช้ `GEMINI_MODEL_CHAT` (ไม่ใช่ `GEMINI_MODEL_DRAFT`)** — งานสร้างคำถามเป็นงานเบากว่าร่าง/วิจารณ์เอกสารมาก ไม่จำเป็นต้องใช้โมเดล reasoning สูง ช่วยคุมต้นทุนของ flow ที่ตอนนี้มี LLM call รวม 3 ครั้งต่อการร่างเอกสาร 1 รอบ (ถาม + ร่าง + scrutinize)

### ผลที่ตามมา (Consequences)

- Endpoint ใหม่ `POST /draft/questions` เพิ่มเข้ามาคู่กับ `POST /draft` เดิม — `/draft` ต้องแก้ signature ให้รับ `answers: dict` เพิ่ม (optional, default `{}`)
- ต้นทุนต่อการร่าง 1 ครั้งเพิ่มจาก 2 เป็น 3 LLM call แต่ใช้โมเดลถูกสำหรับขั้นถามตามข้อ 6 ผลกระทบต้นทุนโดยรวมจึงจำกัด
- UX มีขั้นตอนเพิ่มจากเดิม (2 ปุ่มแทนที่จะเป็น 1 ปุ่ม) แลกกับความแม่นยำที่สูงขึ้น — ยอมรับได้เพราะความถี่การใช้งานโหมดนี้ต่ำอยู่แล้ว (ไม่ใช่โหมดถาม-ตอบหลักที่ใช้บ่อย)
- ต้องมี system prompt ใหม่อีกตัว (`_build_clarify_questions_prompt`) นอกเหนือจาก draft/scrutinize เดิม — เพิ่มจำนวน prompt ที่ต้อง maintain เป็น 4 ตัว (Q&A, clarify, draft, scrutinize) ต้องตั้งชื่อฟังก์ชันให้ชัดเจนต่อไปเพื่อไม่ให้สับสน
- กลไก `[ต้องระบุ: ...]` เป็น convention ใหม่ที่ทั้ง draft prompt และ scrutinize prompt ต้องรู้จักตรงกัน — ถ้าแก้ prompt ใดในอนาคตต้องระวังไม่ให้ทั้งสองฝั่งไม่ตรงกัน (เช่น scrutinize ไม่รู้จักรูปแบบมาร์กนี้แล้วมองข้ามไป)

---

## ADR-003: Auto-fallback โมเดลเมื่อชนโควตาต่อเนื่อง

**สถานะ**: Implemented แล้ว — verify ผ่าน `test_fallback_model.py` แล้วว่า `gemma-4-26b-a4b-it` เรียกผ่าน `GoogleGenAI` ได้จริง (ดู "หมายเหตุการ verify" ท้าย ADR นี้)
**วันที่**: 2026-07-02

### บริบท (Context)

ผู้ใช้หาข้อมูลเพิ่มเติมเรื่องโมเดล Gemma 4 (26B MoE / 31B Dense) เทียบกับ `gemini-3.5-flash` ที่ใช้อยู่ พบว่าแต่ละโมเดลใน Google AI Studio มีโควตาแยกกันเป็นรายโมเดล (RPM/TPM/RPD คนละชุดต่อโมเดล) จึงเสนอแนวคิดว่าน่าจะสลับโมเดลได้เพื่อเลี่ยงการชนโควตา

ระบบมี `GEMINI_MODEL_CHAT`/`GEMINI_MODEL_DRAFT` แยกเป็น env var อยู่แล้ว (ADR-001) และมีกลไก retry เมื่อชนโควตาอยู่แล้ว (`_is_quota_error`, retry 3 ครั้งพร้อม backoff) แต่ยังไม่มีการสลับไปโมเดลอื่นโดยอัตโนมัติ — ถ้า retry ครบ 3 ครั้งแล้วยังชนโควตาอยู่ ระบบจะ error ทันที ทั้งที่มีโมเดลอื่นที่โควตายังไม่หมดให้ใช้ต่อได้

### การตัดสินใจ (Decision)

1. **เพิ่ม `GEMINI_MODEL_CHAT_FALLBACK` และ `GEMINI_MODEL_DRAFT_FALLBACK`** เป็น env var ใหม่ (ค่าเริ่มต้นว่างเปล่า = ไม่มี fallback, พฤติกรรมเดิมทุกประการ) — ผู้ใช้กำหนดเองได้ว่าจะสำรองด้วยโมเดลไหน (เช่น `gemma-4-26b-a4b-it`)

2. **Fallback ทำงานเฉพาะเมื่อ retry โมเดลหลักครบ 3 ครั้งแล้วยังเป็น quota error อยู่เท่านั้น** — ไม่ fallback ทันทีตั้งแต่ error ครั้งแรก และไม่ fallback ถ้า error ไม่ใช่ quota (เช่น network error, invalid prompt) เพราะ error ประเภทอื่นสลับโมเดลก็ไม่ช่วย

3. **ลองโมเดลสำรองแค่ 1 ครั้ง ไม่ retry ซ้ำ** — ถ้าโมเดลสำรองก็ error ด้วย ให้ error กลับไปตามปกติ ไม่วนซ้ำไปมาไม่รู้จบ

4. **สร้าง helper กลาง `_complete_with_fallback()`** ให้ `_handle_clarify_questions`, ขั้นร่าง และขั้น scrutinize ใน `_handle_draft` เรียกใช้ร่วมกัน (ทั้ง 3 จุดนี้เรียก `llm.complete()` แบบเดียวกัน) ส่วน `_handle_chat` ใช้ `chat_engine.chat()` ที่ผูกกับ memory/retriever ต่างออกไป จึงมี fallback logic แยกของตัวเอง แต่หลักการเดียวกัน

### ผลที่ตามมา (Consequences)

- ถ้าไม่ตั้งค่า fallback env var ใดๆ ระบบทำงานเหมือนเดิมทุกประการ (backward compatible)
- ถ้าตั้ง fallback ไว้ คำตอบบางครั้งอาจมาจากคนละโมเดลกันในเซสชันเดียวกันโดยผู้ใช้ไม่รู้ตัว (คุณภาพ/สไตล์การตอบอาจต่างกันเล็กน้อย) — ยอมรับได้เพราะเป้าหมายคือ "ตอบได้ต่อเนื่อง" สำคัญกว่า "ความสม่ำเสมอของสไตล์" ในสถานการณ์ที่ชนโควตา
- ผู้ใช้ต้องตรวจสอบเองว่าโมเดลสำรองที่เลือก (เช่น Gemma 4) ให้คุณภาพเทียบเท่าโมเดลหลักสำหรับงานนั้นๆ ก่อนตั้งค่าไว้จริงจัง — ระบบไม่ได้ตรวจสอบคุณภาพผลลัพธ์ให้อัตโนมัติ

### หมายเหตุการ verify (2026-07-03)

ตอนเขียน ADR นี้ครั้งแรกเดาชื่อโมเดล Gemma 4 ผิด (`gemma-4-26b`/`gemma-4-31b`) — ทดสอบจริงผ่าน `test_fallback_model.py` แล้วพบว่า `GoogleGenAI` คืน `404 NOT_FOUND` เพราะชื่อโมเดลไม่ตรง เช็คกับเอกสารทางการ ([ai.google.dev/gemma/docs/core/gemma_on_gemini_api](https://ai.google.dev/gemma/docs/core/gemma_on_gemini_api)) แล้วพบว่า Gemini API รองรับ Gemma 4 เฉพาะผ่านชื่อที่มี suffix `-it` เท่านั้น:

- `gemma-4-26b-a4b-it` (ไม่ใช่ `gemma-4-26b`)
- `gemma-4-31b-it` (ไม่ใช่ `gemma-4-31b`)

แก้ไขตัวอย่างในเอกสารทุกจุด (`.env.example`, `README.md`, `README_EN.md`, `test_fallback_model.py`) ให้ตรงกับชื่อจริงแล้ว — นี่คือเหตุผลที่ ADR-003 กำชับให้รัน `test_fallback_model.py` ก่อนตั้งค่าใช้งานจริงเสมอ (ดู README) เพราะชื่อโมเดลผิดแม้แค่ตัวเดียวก็ error ทันที

### หมายเหตุเพิ่มเติม (2026-07-03, ตอนทดสอบ ADR-006) — request ไม่เคย timeout เลยสักที่มาก่อน

ระหว่างรัน `test_rag_pipeline.py` จริงครั้งแรกหลัง implement ADR-006/007 พบว่า `/review/target` กับเอกสารจริงที่มี heading เยอะ (18 หัวข้อ) ค้างเกิน client timeout (180s) โดยที่ `rag_worker.log` ไม่มี log บรรทัด "สำเร็จ"/"error" ของ `[REVIEW-CHECKLIST]` ตามมาเลยแม้รอไปอีกหลายนาที — ตรวจสอบแล้วพบว่า `GoogleGenAI(...)` ที่เรียกใน `_complete_with_fallback()`/`_build_chat_engine()` **ไม่เคยตั้ง request timeout เอาไว้เลยตั้งแต่ ADR-001** (`http_options` ไม่ได้ถูกส่งเข้าไป) ทำให้ request ที่ตอบช้าผิดปกติค้างอยู่ใน thread นั้นได้ไม่มีที่สิ้นสุด แทนที่จะ error แล้วปล่อยให้ retry/fallback logic ที่มีอยู่แล้ว (decision ข้างต้น) ทำงาน — log เดิมของระบบเองก็เคยมีร่องรอยนี้อยู่แล้ว (`[DRAFT] สำเร็จใน 171.68s` ในรันก่อนหน้า) แสดงว่า `gemini-3.5-flash` ตอบช้าได้ถึงเกือบ 3 นาทีในบางครั้งโดยไม่ใช่บั๊ก แค่ไม่มีเพดานจับไว้

**แก้โดย**: เพิ่ม `GEMINI_REQUEST_TIMEOUT_MS` (ดีฟอลต์ 300000ms = 5 นาที, ปรับได้ผ่าน `.env`) ผ่าน helper กลาง `_build_llm(model)` ที่ทุกจุดเรียก `GoogleGenAI()` ต้องผ่านตัวนี้แทนการสร้างตรงๆ (แก้ 3 จุด: `_complete_with_fallback` โมเดลหลัก+สำรอง, `_build_chat_engine` ของ `_handle_chat`) — ไม่เปลี่ยน request/response contract ของ endpoint ใดเลย แค่จำกัดเพดานเวลาต่อ 1 call ชัดเจน พร้อมปรับ client-side timeout ใน `app.py`/`test_rag_pipeline.py` ให้กว้างกว่า 5 นาทีนี้ตามไปด้วย (ไม่งั้น client จะ timeout ก่อน worker เอง)

### หมายเหตุเพิ่มเติม (2026-07-03, ต่อจากหมายเหตุ timeout ด้านบน) — ขยาย fallback จาก 1 โมเดลเป็นรายการโมเดล + จัดอันดับจากโควตาจริง

หลังแก้ปัญหา timeout แล้วรัน `test_rag_pipeline.py` ซ้ำ พบว่า `/draft/questions/interactive` เจอ `429 RESOURCE_EXHAUSTED` จริง — โควตารายวันของ `gemini-3.5-flash` บน free tier (`limit: 20` request/วัน ต่อโปรเจกต์ต่อโมเดล) หมดจากการทดสอบสะสมทั้งวัน ผู้ใช้ตรวจแดชบอร์ด Google AI Studio (Rate limits by model) แล้วพบว่า:

- โมเดลตระกูล **"Text-out models"** ทุกตัว (Gemini 3.5/3.1/3/2.5 Flash ทุกรุ่น) แชร์ระดับโควตาใกล้เคียงกันและต่ำมากบน free tier (RPD 20-500 แล้วแต่รุ่น)
- **Gemma 4 26B/31B อยู่คนละหมวด "Other models"** และมี RPD สูงถึง 1.5K/วัน — เป็น **quota pool แยกต่างหาก** จาก Gemini ทั้งหมด จึงเป็น fallback ที่ทนทานที่สุดเมื่อ Gemini ทุกรุ่นชนโควตาพร้อมกัน (สาเหตุที่โควตา Gemini หมดพร้อมกันได้ง่าย เพราะแชร์ pool ระดับหมวดหมู่คล้ายกัน)
- Gemini 3.1 Pro / 2.5 Pro แสดง `0/0` = **ถูกถอดออกจาก free tier แล้วจริง** (ตรงกับที่ ADR-001 เคยบันทึกไว้เชิงหลักการ, ยืนยันซ้ำจากเอกสารทางการ ai.google.dev/gemini-api/docs/models ว่า "Gemini 3 Pro Preview" ถูก shut down แล้วด้วย)

**การตัดสินใจ**: ขยาย `_complete_with_fallback()` และ fallback block ของ `_handle_chat()` จากรองรับโมเดลสำรอง **1 ตัว** เป็น **รายการโมเดลสำรอง** (`GEMINI_MODEL_CHAT_FALLBACK`/`GEMINI_MODEL_DRAFT_FALLBACK` เปลี่ยนจาก string เป็น comma-separated list ผ่าน `_parse_model_chain()`) ไล่ลองทีละตัวตามลำดับจนกว่าจะสำเร็จหรือหมดรายการ — **backward-compatible 100%** กับค่าเดิม (โมเดลเดียวไม่มี comma ยังทำงานเหมือนเดิมทุกประการ) จัดลำดับ 5 โมเดล/ฟังก์ชัน (โมเดลหลัก + สำรอง 4 ตัว) จากข้อมูลโควตาจริงข้างต้น บันทึกไว้ใน `.env`/`.env.example` แล้ว:

- **GEMINI_MODEL_CHAT** (5 อันดับ): `gemini-3.1-flash-lite` (หลัก, RPD 500 สูงสุดแล้ว) → `gemma-4-26b-a4b-it` (คนละ pool) → `gemini-2.5-flash-lite` → `gemini-3-flash-preview` → `gemini-2.5-flash`
- **GEMINI_MODEL_DRAFT** (5 อันดับ): `gemini-3.5-flash` (หลัก, reasoning ดีสุดที่ยังอยู่ใน free tier) → `gemma-4-31b-it` (คนละ pool, dense model ใหญ่กว่า) → `gemma-4-26b-a4b-it` (คนละ pool) → `gemini-3-flash-preview` → `gemini-2.5-flash`

**หมายเหตุการ verify ชื่อโมเดล**: `gemini-2.5-flash`/`gemini-2.5-flash-lite` ตรงตาม official docs ตรงๆ แต่ **"Gemini 3 Flash" ตัวจริงในระบบชื่อ `gemini-3-flash-preview`** (ไม่ใช่ `gemini-3-flash` ที่เดาไว้ตอนแรก — เจอจาก URL slug บนหน้า ai.google.dev/gemini-api/docs/models โดยตรง, เป็น preview model ไม่ใช่ stable) แก้ก่อนใส่ลง `.env` แล้ว ยังไม่ได้รัน `test_fallback_model.py` เพื่อ verify กับ API key จริงของผู้ใช้ (ตามที่ ADR-003 กำชับ) — ควรรันก่อนพึ่งพาโมเดลใหม่ 3 ตัวนี้จริงจัง (`gemini-2.5-flash-lite`, `gemini-3-flash-preview`, `gemini-2.5-flash`)

---

## ADR-004: โหมด Q&A เห็นข้อมูลร่างเอกสารในเซสชันเดียวกัน

**สถานะ**: อนุมัติแล้ว (ยังไม่ implement)
**วันที่**: 2026-07-02

### บริบท (Context)

ผู้ใช้ต้องการให้โหมดถาม-ตอบปกติสามารถนำข้อมูลจากร่างที่เพิ่งสร้างในโหมดร่างเอกสารมาพูดคุยต่อได้ (เช่น "อธิบายข้อ 3 ในร่างที่เพิ่งสร้างให้หน่อย") แต่ระบบ Q&A เดิมมีกฎเหล็กตาม ADR-001 ว่า "ห้ามคิดคำตอบขึ้นมาเองโดยเด็ดขาด ตอบจาก Context ที่ retrieve มาเท่านั้น" ซึ่งเนื้อหาจากโหมดร่างเป็นเนื้อหาที่ AI แต่งขึ้นเอง **ยังไม่ผ่านการอนุมัติจากมนุษย์** การให้ Q&A อ้างอิงเนื้อหานี้ปนกับนโยบายจริงที่อนุมัติแล้วโดยไม่แยกแยะ จะเสี่ยงให้ผู้ใช้เข้าใจผิดว่าร่างเป็นนโยบายที่ใช้งานจริง

### การตัดสินใจ (Decision)

1. **จำกัดขอบเขตแค่ "เซสชันเดียวกัน" เท่านั้น** — ร่างที่สร้างในเซสชัน browser หนึ่งจะไม่ถูกนำไปใช้ตอบคำถามในเซสชันอื่น ไม่บันทึกลง FAISS index ถาวร (ไม่ผสมกับนโยบายจริงที่อนุมัติแล้ว) หมดอายุไปเองเมื่อปิด session (ตรงกับกลไก `_sessions` ที่ผูกกับ `session_id` อยู่แล้ว)

2. **ติดป้ายกำกับชัดเจนทั้งใน data และ UI** — เวลาฉีดร่างเข้า chat memory ของ session จะห่อด้วยข้อความกำกับชัดเจนว่าเป็น "ร่างที่สร้างในโหมดร่างเอกสาร ยังไม่ผ่านการอนุมัติ" และฝั่ง UI (หน้าแชท/ไซด์บาร์) จะมีแบนเนอร์แจ้งว่าเซสชันนี้มีร่างที่ยังไม่อนุมัติอยู่ ป้องกันผู้ใช้สับสนว่าเป็นนโยบายจริง

3. **Implementation: ฉีดเข้า ChatMemoryBuffer ของ session หลัง `/draft` สำเร็จ** — `/draft` ต้องรับ `session_id` เพิ่ม (เดิมไม่มี) เมื่อร่าง+scrutinize เสร็จ ให้เพิ่ม turn สนทนาสังเคราะห์ (synthetic user+assistant message คู่หนึ่ง ติดป้ายกำกับ) เข้า `_sessions[session_id]` เดียวกับที่ `/chat` ใช้ — ไม่ต้องสร้างกลไกเก็บข้อมูลแยกใหม่ ใช้โครงสร้างเดิมที่มีอยู่แล้ว

4. **ไม่แก้ `_build_sys_prompt()` (กฎเหล็ก Q&A เดิม)** — กฎ "ห้ามแต่งคำตอบเอง" ยังคงอยู่ครบ สิ่งที่เปลี่ยนคือ Q&A จะ "เห็น" ร่างในประวัติสนทนา (เหมือนที่เห็นคำถาม-คำตอบก่อนหน้าของผู้ใช้เองในเซสชัน) ไม่ใช่การเปลี่ยนกฎ grounding ของ retrieval

### ผลที่ตามมา (Consequences)

- `/draft` endpoint เปลี่ยน signature อีกครั้ง (เพิ่ม `session_id`) — ครั้งที่ 3 แล้วนับจาก ADR-001 (เพิ่ม endpoint) และ ADR-002 (เพิ่ม `answers`) endpoint นี้มีแนวโน้มขยาย signature เรื่อยๆ ตามฟีเจอร์ใหม่ ควรพิจารณา schema แบบ versioned หรือรวม field ที่ยังไม่ใช้เป็น `dict` เปิดกว้างไว้ล่วงหน้า ถ้ามีฟีเจอร์เพิ่มอีกในอนาคต
- แม้ระบบจะไม่แก้กฎเหล็กของ Q&A แต่โดยธรรมชาติของ `condense_plus_context` chat mode LLM จะเห็นประวัติสนทนาทั้งหมดรวมถึงร่างที่ฉีดเข้าไป จึงมีโอกาสที่คำตอบจะอ้างอิงเนื้อหาร่างโดยตรงเมื่อผู้ใช้ถามถึง — เป็นพฤติกรรมที่ต้องการ (ตามคำขอ) แต่ต้องพึ่งป้ายกำกับที่ชัดเจนในข้อ 2 เพื่อกันความเข้าใจผิด ไม่ใช่การบังคับด้วยกฎเหล็กแบบเดียวกับ retrieval
- ถ้าผู้ใช้สร้างร่างหลายหัวข้อในเซสชันเดียว ทุกร่างจะสะสมอยู่ใน chat memory (จำกัดด้วย `token_limit=8000` ที่มีอยู่แล้ว) ร่างเก่าอาจถูกตัดออกอัตโนมัติเมื่อประวัติยาวเกิน ไม่ต้องจัดการเพิ่ม

---

## ADR-005: Session eviction (ลบ session ที่ไม่ได้ใช้งานนาน)

**สถานะ**: อนุมัติแล้ว และ implement แล้ว
**วันที่**: 2026-07-03

### บริบท (Context)

`/scrutinize` skill ตรวจพบว่า `_sessions` dict ใน `rag_worker.py` (เก็บ `ChatMemoryBuffer` ต่อ session สำหรับทั้ง `/chat` และการฉีดร่างตาม ADR-004) ไม่มีกลไกลบ session ที่ไม่ได้ใช้งานแล้วออกเลย ทุก browser tab ที่เปิดแอปสร้าง session ใหม่ที่ค้างอยู่ใน memory ของ worker ตลอดอายุ process และหลัง ADR-004 แต่ละ session มีโอกาสหนักขึ้นกว่าเดิม เพราะอาจมีข้อความร่าง+scrutiny ยาวๆ ถูกฉีดเข้าไปด้วย ถ้า worker รันต่อเนื่องนานหลายวัน/สัปดาห์โดยไม่ restart และมีคนใช้งานเยอะ memory จะโตขึ้นเรื่อยๆ ไม่มีเพดาน

### การตัดสินใจ (Decision)

1. **เพิ่ม `SESSION_IDLE_TIMEOUT_SECONDS`** เป็น env var ใหม่ (ดีฟอลต์ 28800 วินาที = 8 ชั่วโมง) กำหนดว่า session ที่ไม่ได้ใช้งานนานเกินนี้จะถือว่าหมดอายุ

2. **เก็บ timestamp การใช้งานล่าสุดแยกต่างหาก** ใน `_session_last_used: dict[str, float]` คู่ขนานกับ `_sessions` — อัปเดตทุกครั้งที่ session ถูกใช้งาน (ทั้งจาก `_handle_chat` และ `_inject_draft_into_session`) ภายใต้ lock เดียวกับที่แก้ `_sessions` อยู่แล้ว (`_sessions_lock`) ไม่สร้าง lock ใหม่ เพื่อกัน deadlock จากการล็อกซ้อน

3. **รัน background thread แยก (`_cleanup_idle_sessions`)** ตื่นทุก 10 นาที ลบ session ที่ idle เกิน threshold ออกจากทั้ง `_sessions` และ `_session_last_used` พร้อมกัน — เลือกช่วง 10 นาทีเพราะ threshold หลักเป็นหน่วยชั่วโมง ไม่จำเป็นต้องเช็คถี่กว่านี้

4. **ไม่แจ้งเตือนผู้ใช้เมื่อ session หมดอายุ** — ถ้าผู้ใช้กลับมาใช้แชทหลัง session หมดอายุไปแล้ว ระบบจะสร้าง `ChatMemoryBuffer` ใหม่เปล่าๆ ให้อัตโนมัติเหมือน session ใหม่ (พฤติกรรมเดิมของระบบตอน session_id ยังไม่เคยมีอยู่) ไม่มี error ให้เห็น เพียงแต่ประวัติสนทนา/ร่างเก่าในเซสชันนั้นจะหายไป

### ผลที่ตามมา (Consequences)

- ค่าเริ่มต้น 8 ชั่วโมงเป็นการประมาณคร่าวๆ ยังไม่มีข้อมูลการใช้งานจริงมายืนยันว่าเหมาะสม ถ้าพบว่าผู้ใช้งานเปิดแท็บทิ้งไว้ข้ามคืนแล้วกลับมาคุยต่อบ่อย อาจต้องปรับค่านี้ให้นานขึ้น
- Background thread ที่สองนี้ (นอกจาก `loader_thread`) เพิ่ม thread ที่รันตลอดอายุ worker process — เป็น daemon thread เหมือนกัน ไม่บล็อกการปิดโปรแกรม
- ยังไม่มี metric/log สรุปว่าปกติมีกี่ session ค้างอยู่ ณ ขณะใดขณะหนึ่ง — ถ้าต้องการ observability เพิ่มเติมในอนาคต (เช่น endpoint `/stats`) ยังไม่ได้ทำในรอบนี้

---

## ADR-006: เพิ่มโหมด "รีวิวเอกสาร" (Document Review Mode) แยกจากโหมดร่างเอกสาร

**สถานะ**: Implemented แล้ว (2026-07-03 เซสชันถัดจาก HANDOFF.md) — **ยังไม่ได้รัน `test_rag_pipeline.py`
จริงในเครื่องผู้ใช้** (sandbox ของ agent ต่อ `localhost:8765`/venv ของผู้ใช้ไม่ได้ — ดู HANDOFF.md
"Sandbox constraint") ผู้ใช้ต้องรัน `venv\Scripts\python.exe test_rag_pipeline.py` เองก่อน merge/deploy
จริง เทสต์ใหม่ 7-9 ครอบคลุม `/review/target` (ทั้งเคสสำเร็จและเคสถูกปฏิเสธ) และ `/review/topic`
**วันที่**: 2026-07-03

### บริบท (Context)

ผลจาก `/scrutinize` ปนกับการทดลองใช้จริงของผู้ใช้: ผู้ใช้ลองพิมพ์คำว่า "รีวิว" ในช่อง "หัวข้อนโยบาย" ของโหมดร่างเอกสาร (ADR-001/002) โดยคาดหวังให้ระบบเดินตรวจเอกสารนโยบายเดิมของตัวเองทีละหัวข้อ แต่โหมดร่างเอกสารถูกออกแบบมาสำหรับ "แต่งเอกสารใหม่ทั้งฉบับ + ถามเฉพาะช่องว่างที่ context ไม่ครอบคลุมเลย" เท่านั้น ไม่มี concept ของ "เอกสารเป้าหมายที่จะรีวิว" อยู่ในระบบเลย — `_retrieve_context()` ทำ semantic search ข้าม corpus ทั้งหมดแบบไม่ scope ต่อเอกสารใดเอกสารหนึ่ง ผลคือคำถามที่ได้เป็นคำถามทั่วไปเกี่ยวกับช่องว่าง ไม่ใช่การเดินตรวจเนื้อหาที่มีอยู่แล้วทีละหัวข้อตามที่ผู้ใช้ต้องการ และร่างที่ได้เอาเนื้อหาจาก corpus (นโยบายเดิมของบริษัทก่อนเปลี่ยนชื่อจาก "ทเวนตี้ โฟร์ คอน แอนด์ ซัพพลาย" เป็น "ออริจิ้น โกลบอล เอ็มไพร์") มาแทรกโดยไม่มีการยืนยันจากผู้ใช้ก่อน

ผู้ใช้ยืนยันชัดเจนว่าต้องการฟีเจอร์ "รีวิวเอกสาร" แยกเป็นฟีเจอร์ใหม่จริงจัง ไม่ใช่แค่ปรับโหมดร่างเอกสารเดิม

### การตัดสินใจ (Decision)

1. **เพิ่มโหมดใหม่ "โหมดรีวิวเอกสาร" แยกจากโหมดร่างเอกสารเดิมโดยสิ้นเชิง** — ไม่แก้ ADR-001/002 ไม่ใช้ endpoint `/draft*` ร่วมกัน เพราะ state model ต่างกัน (ทีละหัวข้อรีวิว + ย้อนกลับได้ เทียบกับฟอร์มเดียวจบของ Draft mode)

2. **รับเอกสารเป้าหมายได้ 2 ทาง**: อัปโหลดไฟล์ใหม่ตรง (ไม่ต้อง index ก่อน) หรือเลือกจากเอกสารที่ index ไว้แล้วใน corpus

2a. **นโยบายปฏิเสธไฟล์ที่ parse heading ไม่ได้**: ถ้า parse โครงสร้าง heading ของเอกสารเป้าหมายไม่ได้เลย (เอกสารเขียนเป็นพรืดเดียวไม่มีหัวข้อ, PDF สแกนภาพ, หรือฟอร์แมตที่ parser ไม่รองรับ) ให้ปฏิเสธทันทีด้วยข้อความแจ้งเตือนชัดเจนว่าไม่รองรับเอกสารประเภทนี้ในโหมดรีวิว แนะนำให้ไปใช้โหมดร่างเอกสารแทน — ไม่ fallback เป็น checklist ล้วนๆ เงียบๆ เพราะจะเป็นการรีวิวที่ไม่ครอบคลุมเนื้อหาจริงของเอกสารเลย ทำให้ผู้ใช้เข้าใจผิดว่ารีวิวครบแล้ว

3. **ขอบเขตการรีวิว = เอกสารเป้าหมาย 1 ฉบับ + เอกสารที่เกี่ยวข้อง** (Cross-reference documents — ดู CONTEXT.md) ที่ระบบ auto-suggest จาก retrieval มาให้ก่อน แล้วผู้ใช้ยืนยัน/เพิ่มเองได้ — เข้าถึงเอกสารเป้าหมายผ่าน **Target document access** และเทียบกับเอกสารอื่นผ่าน **Cross-reference retrieval** (นิยามแยกกันชัดเจน ดู CONTEXT.md) ไม่ดึงข้าม corpus แบบสุ่มเหมือน Draft mode เดิม เพื่อไม่ให้เนื้อหาจากเอกสารอื่นที่ไม่เกี่ยวข้องหลุดเข้ามาปนโดยไม่ได้ตั้งใจ

4. **หัวข้อรีวิว (Review Topic — ดู CONTEXT.md) มาจาก 2 แหล่งผสมกัน**: heading จริงของเอกสารเป้าหมาย + checklist เฉพาะประเภทเอกสารสำหรับสิ่งที่เอกสารเดิมไม่มีเลย (ใช้กลไกสร้างคำถามแบบเดียวกับ ADR-002 ต่อยอด) **checklist-derived topic ต้อง skip เรื่องที่ heading-derived topic ครอบคลุมไปแล้ว** (สืบทอดกฎ dedup เดิมของ ADR-002 ข้อ "ถ้า Context มีข้อมูลที่เกี่ยวข้องอยู่แล้ว ห้ามถามซ้ำ" มาใช้ระหว่าง 2 แหล่งนี้ด้วย ไม่ใช่แค่ระหว่าง checklist กับ context เดิมแบบ ADR-002) **follow-up question (ถามต่อตามคำตอบก่อนหน้า แบบเดียวกับ ADR-007 decision 2) ใช้ได้เฉพาะกับ checklist-derived topic เท่านั้น** — heading-derived topic ไม่มี follow-up เพราะเป้าหมายของมันคือยืนยัน/แก้เนื้อหาที่มีอยู่แล้วในเอกสาร (ผ่าน Prefill) ไม่ใช่ขุดข้อมูลใหม่เป็นชั้นๆ แบบ checklist-derived

5. **ถามทีละหัวข้อรีวิว ไม่ใช่ฟอร์มเดียวแสดงทุกข้อพร้อมกัน** — ย้อนกลับไปแก้คำตอบก่อนหน้าได้ และก่อนถามแต่ละหัวข้อ ระบบ **Prefill** คำตอบที่น่าจะเป็นจาก context มาเสนอให้ผู้ใช้ยืนยัน/แก้ก่อนเสมอ (ดูกฎบังคับร่วมของ Prefill ใน CONTEXT.md) — นี่คือจุดที่แก้ปัญหาต้นตอของ ADR นี้โดยตรง (ดูบริบท)

6. **ผลลัพธ์ = รายงานสรุปการเปลี่ยนแปลงทีละหัวข้อ + เอกสารฉบับปรับปรุงคู่กัน** ไม่ใช่แค่รายงานเฉยๆ หรือแค่เอกสารใหม่เฉยๆ

7. **ไม่ทำ save/resume ข้ามเซสชัน** — ถ้าหลุดระหว่างทาง (refresh/ไฟดับ) ให้เริ่มรีวิวใหม่ได้เลย ตัดสินใจแล้วว่าความซับซ้อนของการ persist ไม่คุ้มกับความถี่การใช้งานฟีเจอร์นี้

8. **ใช้ `GEMINI_MODEL_DRAFT` ไม่ใช้ `GEMINI_MODEL_CHAT`** สำหรับสร้างหัวข้อรีวิวและ follow-up ของ checklist-derived topic (ดู decision ข้อ 4) เพราะงานซับซ้อนกว่าคำถามเดิมมาก (ต้องอ่านเอกสารเป้าหมายทั้งฉบับ + เทียบกับเอกสารที่เกี่ยวข้อง + ตั้งคำถามเจาะจงทีละหัวข้อ)

9. **Endpoint ใหม่ทั้งหมดของโหมดนี้ ไม่แตะ `/draft*` เดิมเลยแม้แต่บรรทัดเดียว** — คนละชุด endpoint คนละ contract ทดสอบแยกจาก `test_rag_pipeline.py` เดิม (เพิ่มเทสต์ใหม่ ไม่ต้องแก้เทสต์เก่า) ตัวอย่าง endpoint: `POST /review/target` (ส่งเอกสารเป้าหมาย → คืนรายการ Review Topic + รายการเอกสารที่เกี่ยวข้องที่ auto-suggest ไว้ให้ยืนยัน/แก้ ในคำตอบเดียวกัน ไม่แยก endpoint) และ `POST /review/topic` (ถามทีละหัวข้อ พร้อม prefill)

### ผลที่ตามมา (Consequences)

- ต้องเพิ่ม metadata filter ตอน retrieve (ปัจจุบัน `build_index.py` ใส่แค่ `file_name` ผ่าน `SimpleDirectoryReader` ไม่มี filter ต่อเอกสารเลย) เป็นงาน infra ใหม่ที่ยังไม่มีในระบบ — ใช้กับ **Cross-reference retrieval** เท่านั้น ส่วน **Target document access** (เอกสารที่อัปโหลดสดยังไม่ index) ต้อง parse ไฟล์ดิบตรงๆ แยกจากเส้นทาง FAISS โดยสิ้นเชิง คนละกลไกกันตั้งแต่ต้น
- ต้องมีกลไก parse โครงสร้าง heading ของเอกสารเป้าหมาย ทั้งจากเอกสาร markdown ที่ index อยู่แล้ว และจากไฟล์ที่อัปโหลดสด (เช่น .docx ต้องอ่านจาก paragraph style "Heading 1/2/3" ผ่าน python-docx ไม่ใช่ "## " แบบ markdown) — สองเส้นทางใช้ parser คนละตัวแน่นอน ไม่ใช่แค่ "อาจจะ" ต้องระบุไว้ตั้งแต่ตอน implement
- ต้นทุนต่อการรีวิว 1 เอกสาร (10-25 หัวข้อรีวิว x อย่างน้อย 1 LLM call ต่อหัวข้อสำหรับ prefill เสมอ + อีก 1 call เพิ่มเฉพาะหัวข้อที่เป็น checklist-derived สำหรับ follow-up ตาม decision ข้อ 4) สูงกว่าการร่างใหม่ 1 ครั้งมาก — **ไม่ใช่ประเด็นที่ต้องบล็อก go-live**: ระบบนี้ใช้ API key ของผู้ใช้เอง (ไม่ใช่ค่าใช้จ่ายส่วนกลางที่ต้องขออนุมัติ) และมี Auto-fallback (ดู ADR-003) รองรับกรณีชนโควตาจากปริมาณ call ที่เพิ่มขึ้นอยู่แล้ว — ไม่ต้องเปิดประเด็น budget ใหม่
- **State ของการรีวิวทีละหัวข้อเก็บฝั่ง client เท่านั้น ไม่มี server-side session ใหม่**: endpoint ใหม่ (decision ข้อ 9) เป็น stateless — แต่ละ request ส่งหัวข้อทั้งหมด + คำตอบที่ตอบไปแล้วทั้งก้อนมาด้วยทุกครั้ง (รูปแบบเดียวกับที่ `/draft` ส่ง `answers: dict` อยู่แล้วทุกวันนี้) เก็บสถานะไว้ใน `st.session_state` ฝั่ง Streamlit เหมือน `draft_clarify`/`draft_result` ที่มีอยู่แล้ว — สอดคล้องกับ decision ข้อ 7 ที่ตัดสินใจไม่ทำ save/resume ข้ามเซสชันอยู่แล้ว จึงไม่จำเป็นต้องมี `_review_sessions` dict ใหม่ฝั่ง worker แบบที่ ADR-005 เคยต้องทำ cleanup ให้ `_sessions` — ใช้ state model เดียวกันนี้ร่วมกับ ADR-007 ด้วย ไม่ทำแยกกัน 2 แบบ แต่ **ไม่ใช้ endpoint ตัวเดียวกับ `/draft/questions` เดิม** (ดู decision ข้อ 9)

---

## ADR-007: ขยายคำถามเพิ่มเติมก่อนร่างเอกสาร (ต่อยอด ADR-002) ให้ละเอียดขึ้น 2-5 เท่า

**สถานะ**: Implemented แล้ว (2026-07-03 เซสชันถัดจาก HANDOFF.md) — **ยังไม่ได้รัน `test_rag_pipeline.py`
จริงในเครื่องผู้ใช้** เช่นเดียวกับ ADR-006 (ดู หมายเหตุสถานะของ ADR-006 ด้านบน) เทสต์ใหม่ข้อ 10
ครอบคลุม `/draft/questions/interactive` ทั้งการเรียกครั้งแรก (สร้างคำถามจัดหมวดหมู่) และการขอ prefill
**วันที่**: 2026-07-03

### บริบท (Context)

ทดลองใช้โหมดร่างเอกสารจริงแล้วพบว่าคำถามเพิ่มเติม 3-6 ข้อตาม ADR-002 (เฉพาะจุดที่ context ไม่ครอบคลุมเลย) ไม่ละเอียดพอ — ร่างที่ได้เอาเนื้อหาจากนโยบายเดิมในนามบริษัทก่อนเปลี่ยนชื่อมาใช้แทบทั้งหมดโดยไม่มีการถามยืนยันจากผู้ใช้เลยว่าเนื้อหานั้นยังใช้ได้จริงกับสถานการณ์ปัจจุบันหรือไม่ ทำให้ "ผลลัพธ์ใช้งานจริงไม่ได้" ผู้ใช้ต้องการคำถามที่ละเอียดกว่าเดิม 2-5 เท่า ทั้งจำนวนหัวข้อ ความลึกต่อหัวข้อ (follow-up) และการจัดกลุ่มหมวดหมู่

### การตัดสินใจ (Decision)

1. **ขยายจำนวนคำถามจาก 3-6 เป็น ~10-25 ข้อ** จัดกลุ่มเป็นหมวดหมู่ตามหัวข้อนโยบาย (เช่น ขอบเขต / บทบาทและความรับผิดชอบ / ตัวชี้วัด / การอนุมัติ) แทนที่จะเป็น list เดี่ยวไม่มีหมวดหมู่เหมือนเดิม

2. **เพิ่ม follow-up question ตามคำตอบก่อนหน้า** — ไม่ใช่ถามครั้งเดียวจบเหมือน ADR-002 เดิม (เช่น ตอบ "ทบทวนปีละ 1 ครั้ง" ระบบถามต่อว่า "ใครเป็นผู้รับผิดชอบทบทวน")

3. **เปลี่ยน UI จากฟอร์มเดียวโชว์ทุกข้อพร้อมกัน (`app.py` stage 2 เดิม) เป็นถามทีละข้อ** ย้อนกลับแก้คำตอบก่อนหน้าได้ พร้อม **Prefill** จาก context ก่อนถามทุกครั้ง (ใช้กฎบังคับร่วมของ Prefill ใน CONTEXT.md เต็มรูปแบบเช่นกัน — เป็นจุดแก้ปัญหาต้นตอของ ADR นี้เอง ดูบริบท ไม่ใช่แค่ของ ADR-006) — reuse แค่ **UI/state model เดียวกับโหมดรีวิวเอกสาร (ADR-006)** เท่านั้น (state เก็บฝั่ง client/`st.session_state` แบบเดียวกัน ไม่มี server-side session ใหม่ — ดู ADR-006 ผลที่ตามมา) **ไม่ reuse นิยามเต็มของ "หัวข้อรีวิว"** เพราะโหมดร่างเอกสารไม่มีเอกสารเป้าหมายให้ดึง heading จริงเหมือน ADR-006 (กำลังแต่งเอกสารที่ยังไม่เคยมีอยู่) หัวข้อของโหมดนี้จึงเป็น checklist-derived ล้วนๆ เสมอ — ยืมแค่กลไก "ถามทีละข้อ + ย้อนกลับได้ + prefill" มาใช้ ไม่ใช่ยืมคำนิยามทั้งก้อน

4. **อัปเกรดโมเดลสร้างคำถามจาก `GEMINI_MODEL_CHAT` เป็น `GEMINI_MODEL_DRAFT`** เพราะงานจัดกลุ่มหมวดหมู่ + follow-up ต้องการ reasoning มากกว่าถามครั้งเดียวจบแบบเดิม

5. **ไม่แก้ `POST /draft/questions` เดิม** — คงพฤติกรรม/contract เดิมไว้ทั้งหมด (`test_rag_pipeline.py:191` ยัง assert รูปแบบเดิมได้ ไม่ต้องแก้เทสต์เก่า) เพิ่ม endpoint ใหม่แยกต่างหาก (เช่น `POST /draft/questions/interactive`) สำหรับ flow ทีละข้อ+follow-up นี้โดยเฉพาะ — เป็นการตอบรับคำแนะนำที่ ADR-004 เคยเตือนไว้เรื่อง endpoint นี้ขยาย signature เรื่อยๆ (ดู ADR-004 ผลที่ตามมา) ด้วยการแยก endpoint ใหม่แทนการขยาย signature เดิมซ้ำอีกครั้ง

### ผลที่ตามมา (Consequences)

- ต้นทุนโหมดร่างเอกสารเพิ่มขึ้นชัดเจน — เดิม 2 LLM call ต่อคำขอ (ร่าง + scrutinize) กลายเป็นอย่างน้อย 2 + N call (N = จำนวนรอบคำถาม/follow-up) และขั้นตอนคำถามก็ใช้โมเดลแพงขึ้นด้วย — **ไม่ใช่ประเด็นที่ต้องบล็อก go-live** เช่นเดียวกับ ADR-006 (API key ของผู้ใช้เอง + มี Auto-fallback ตาม ADR-003 รองรับอยู่แล้ว)
- มี endpoint คำถามก่อนร่าง 2 ตัวคู่ขนาน (`/draft/questions` เดิมแบบยิงครั้งเดียว กับ `/draft/questions/interactive` ใหม่แบบทีละข้อ) — เพิ่มพื้นผิวโค้ดที่ต้อง maintain แต่แลกกับไม่พังของเดิม ถ้าในอนาคตจะยกเลิก endpoint เดิมจริง ต้องเปิด ADR ใหม่แยกเพื่อ deprecate อย่างเป็นทางการ (ยังไม่ตัดสินใจตอนนี้)
- ผู้ใช้ต้องตอบคำถามเยอะขึ้น 2-5 เท่าก่อนได้ร่าง เวลาที่ใช้ต่อครั้งนานขึ้นมาก — **ไม่ต้อง monitor อัตราล้มเลิกกลางทางแบบผลิตภัณฑ์สาธารณะ** เพราะระบบนี้ไม่ได้แจกให้คนทั่วไปใช้ฟรี เป็นผู้ใช้ภายในที่ใช้ API key ของตัวเอง ไม่มีความเสี่ยงเรื่อง user churn ที่ต้องเฝ้าระวัง
