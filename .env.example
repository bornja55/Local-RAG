# คัดลอกไฟล์นี้เป็น .env แล้วใส่ค่าจริงของคุณ (.env ไม่ถูก commit เข้า git)
GOOGLE_API_KEY=your-gemini-api-key-here

# ชื่อองค์กร/บริษัท — ใช้โชว์บนหน้าเว็บและใส่ใน system prompt ของ LLM
# ไม่บังคับ ถ้าไม่ตั้งจะขึ้นเป็น "องค์กรของคุณ"
COMPANY_NAME=บริษัทของคุณ จำกัด

# โมเดล Gemini แยกตามโหมด — ไม่บังคับ มีค่า default ในตัวโค้ดอยู่แล้ว
# GEMINI_MODEL_CHAT: ใช้กับโหมดถาม-ตอบปกติ (retrieval ล้วน โมเดลเบา/ถูกพอ)
# GEMINI_MODEL_DRAFT: ใช้กับโหมดร่างเอกสาร+scrutinize (ต้อง reasoning ดีกว่า)
GEMINI_MODEL_CHAT=gemini-3.1-flash-lite
GEMINI_MODEL_DRAFT=gemini-3.5-flash

# โมเดลสำรอง (fallback) — ไม่บังคับ ค่า default คือค่าว่าง (= ไม่มี fallback, ปิดฟีเจอร์นี้)
# ถ้าตั้งค่าไว้ ระบบจะไล่สลับไปใช้โมเดลเหล่านี้โดยอัตโนมัติทีละตัวตามลำดับ เฉพาะตอนโมเดลหลัก (ด้านบน)
# ชนโควตา (429/quota) ต่อเนื่องครบ 3 ครั้ง retry แล้วเท่านั้น ไม่ fallback ทันทีตั้งแต่ error ครั้งแรก
# (ดู ADR-003) รองรับหลายโมเดลคั่นด้วย comma — ตัวอย่างด้านล่างจัดอันดับ 4 โมเดลสำรอง/ฟังก์ชัน จาก
# โควตาจริงใน Google AI Studio (Gemma 4 มี quota pool แยกจาก Gemini "Text-out models" เลย เหมาะเป็น
# ตัวแรกในรายการ — ดู ADR-003 หมายเหตุเพิ่มเติม 2026-07-03) ทุกชื่อโมเดลด้านล่าง verify กับ
# ai.google.dev/gemini-api/docs/models แล้ว แต่แนะนำให้รัน test_fallback_model.py <model_name> เช็ค
# กับ API key ของคุณเองอีกทีก่อนพึ่งพาจริงจัง (ชื่อโมเดลผิดแม้แค่ตัวเดียวก็ error ทันที):
GEMINI_MODEL_CHAT_FALLBACK=gemma-4-26b-a4b-it,gemini-2.5-flash-lite,gemini-3-flash-preview,gemini-2.5-flash
GEMINI_MODEL_DRAFT_FALLBACK=gemma-4-31b-it,gemma-4-26b-a4b-it,gemini-3-flash-preview,gemini-2.5-flash

# หมดอายุ session แชท/ร่างที่ไม่ได้ใช้งานนาน (วินาที) — ไม่บังคับ ค่า default คือ 28800 (8 ชั่วโมง)
# ป้องกัน memory ของ worker โตไม่มีเพดานถ้าปล่อยรันต่อเนื่องนานๆ (ดู ADR-005)
SESSION_IDLE_TIMEOUT_SECONDS=28800

# Timeout ต่อ 1 request ที่ยิงไป Gemini API (หน่วยมิลลิวินาที) — ไม่บังคับ ค่า default คือ 300000
# (5 นาที) กันไม่ให้ request ค้างไม่มีที่สิ้นสุด (เจอระหว่างทดสอบโหมดรีวิวเอกสาร ADR-006 ว่าบางครั้ง
# gemini-3.5-flash ตอบช้าถึง ~170 วินาที) ถ้าลดค่านี้ต่ำเกินไป request ปกติที่แค่ตอบช้าอาจโดนตัดก่อนเวลา
GEMINI_REQUEST_TIMEOUT_MS=300000
