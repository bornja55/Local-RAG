# CONTEXT.md

โครงสร้างศัพท์และภาพรวมของโปรเจกต์ Policy RAG Assistant — อัปเดตล่าสุดตอนออกแบบฟีเจอร์ "โหมดร่างเอกสาร + Scrutinize" (ก.ค. 2026)

## ภาพรวมระบบ

แอปถาม-ตอบนโยบายองค์กรด้วย RAG (Retrieval-Augmented Generation) แบ่งเป็น 2 โปรเซส: `app.py` (หน้าเว็บ Streamlit) และ `rag_worker.py` (โปรเซสเบื้องหลังที่ถือโมเดล AI ทั้งหมด) คุยกันผ่าน local HTTP `127.0.0.1:8765` ดูรายละเอียดสถาปัตยกรรมเต็มใน README.md

## Glossary

- **Worker**: หมายถึง `rag_worker.py` — โปรเซสพื้นหลังที่ถือ embedding model, reranker, FAISS index และเป็นตัวเรียก Gemini API ทั้งหมด แยกขาดจากโปรเซส Streamlit เพื่อป้องกัน native-library crash บน Windows

- **Grounding**: การบังคับให้ LLM ตอบโดยอ้างอิงเฉพาะเนื้อหาที่ retrieval ดึงมาให้เท่านั้น ห้ามแต่งคำตอบขึ้นเองนอกเหนือจาก context ที่ให้ไป — เป็นหลักการที่ระบบ Q&A เดิมใช้เพื่อป้องกันการตอบมั่ว (hallucination)

- **Iron-rule system prompt** (กฎเหล็ก): system prompt เดิมใน `_build_sys_prompt()` ที่บังคับ grounding แบบเข้มงวด (`"ห้ามคิดคำตอบขึ้นมาเองโดยเด็ดขาด"`) ใช้กับโหมด Q&A ปกติเท่านั้น — โหมดร่างเอกสารใหม่จะไม่ใช้กฎนี้ (ดู ADR-001)

- **Draft mode (โหมดร่างเอกสาร)**: ฟีเจอร์ใหม่ที่ผู้ใช้สั่งให้ระบบ "แต่ง" เอกสารนโยบายใหม่ทั้งฉบับ (ไม่ใช่แค่ค้นหาของเดิม) โดยยังคงดึง context จากเอกสารที่มีอยู่เพื่ออ้างอิง/ให้สอดคล้องกัน แต่ปลดกฎ "ห้ามแต่งคำตอบเอง" ออก เพราะเป้าหมายของโหมดนี้คือการแต่งเนื้อหาใหม่โดยนิยาม

- **Scrutinize / Auto-scrutinize**: ขั้นตอนตรวจสอบอัตโนมัติที่รันต่อทันทีหลัง draft mode สร้างร่างเอกสารเสร็จ — ให้ LLM (โมเดลเดียวกับที่ใช้ร่าง) วิจารณ์ร่างของตัวเองเทียบกับเอกสารนโยบายที่มีอยู่จริง (ผ่าน context เดียวกัน) หาความขัดแย้ง ช่องว่าง หรือจุดที่ต้องระวัง

- **CRITICAL / WARNING / NITPICK / VERDICT**: ระดับความรุนแรงของประเด็นที่ scrutinize รายงาน (มาตรฐานเดียวกับ `/scrutinize` skill) — CRITICAL คือขัดแย้งกับนโยบายเดิมชัดเจนหรือความเสี่ยงสูง, WARNING คือควรตรวจสอบก่อนใช้จริง, NITPICK คือจุดเล็กน้อยไม่กระทบสาระ, VERDICT คือสรุปภาพรวม 1 บรรทัด

- **CRITICAL gate**: นโยบายของระบบว่าจะทำอะไรเมื่อ scrutinize เจอปัญหาระดับ CRITICAL — ตัดสินใจแล้วว่า **ไม่บล็อกการดาวน์โหลด** แค่โชว์คำเตือนเด่นชัด (ดู ADR-001)

- **COMPANY_NAME**: env var ใน `.env` กำหนดชื่อองค์กรที่จะแทรกในหน้าเว็บและ system prompt — ทำให้ codebase deploy ให้องค์กรอื่นได้โดยไม่แก้โค้ด

- **GEMINI_MODEL_CHAT / GEMINI_MODEL_DRAFT**: env var แยก model ที่ใช้ระหว่างโหมด Q&A ปกติ (`gemini-3.1-flash-lite` — ถูก เร็ว พอสำหรับงาน retrieval ล้วน) กับโหมดร่าง+scrutinize (`gemini-3.5-flash` — แพงกว่าแต่ reasoning/expert-task ดีกว่า จำเป็นสำหรับงานแต่งเอกสาร+วิจารณ์ตัวเอง) — `GEMINI_MODEL_CHAT` ยังถูกใช้ซ้ำในขั้นตอนสร้างคำถามเพิ่มเติมด้วย (ดู ADR-002)

- **Clarifying questions (คำถามเพิ่มเติม)**: ขั้นตอนใหม่ก่อนร่างเอกสาร (ดู ADR-002) — AI สร้างคำถาม 3-6 ข้อแบบเฉพาะเจาะจงตามหัวข้อที่ผู้ใช้ระบุ โดยดู context ที่ retrieve ได้ก่อนแล้วถามเฉพาะจุดที่ context ไม่ครอบคลุม (เช่น หัวข้อ "นโยบาย PDPA" จะถามเรื่องข้อมูลส่วนบุคคลที่เก็บ/DPO ที่ไม่มีอยู่ในนโยบาย IT/Risk Management เดิม) เรียกผ่าน endpoint `POST /draft/questions`

- **`[ต้องระบุ: ...]` marker**: รูปแบบมาร์กที่ตกลงร่วมกันระหว่าง draft prompt กับ scrutinize prompt (ดู ADR-002 ข้อ 4) — ใช้เมื่อผู้ใช้ข้ามคำถามเพิ่มเติมข้อใดไม่ตอบ AI จะใส่เครื่องหมายนี้ตรงจุดที่เกี่ยวข้องในร่างแทนการเดาคำตอบเอง แล้ว scrutinize จะยกจุดที่ยังมีมาร์กนี้ค้างอยู่ขึ้นเป็น WARNING เสมอ

- **Auto-fallback (โมเดลสำรอง)**: กลไกสลับไปใช้โมเดล Gemini อื่นโดยอัตโนมัติ เฉพาะเมื่อโมเดลหลัก retry ครบ 3 ครั้งแล้วยังชนโควตา (quota/429) อยู่เท่านั้น (ดู ADR-003) ตั้งค่าผ่าน `GEMINI_MODEL_CHAT_FALLBACK` / `GEMINI_MODEL_DRAFT_FALLBACK` ใน `.env` — ค่าเริ่มต้นว่างเปล่าเท่ากับปิดฟีเจอร์นี้ ใช้ helper กลาง `_complete_with_fallback()` ร่วมกันในทุกจุดที่เรียก `llm.complete()` (clarify, draft, scrutinize) ส่วน `_handle_chat` มี fallback logic แยกของตัวเองเพราะใช้ `chat_engine.chat()` แทน

- **Chat เห็นร่างในเซสชัน**: โหมดถาม-ตอบปกติสามารถอ้างอิงเนื้อหาจากร่างที่เพิ่งสร้างในโหมดร่างเอกสารได้ แต่จำกัดขอบเขตแค่ "เซสชันเดียวกัน" เท่านั้น (ไม่บันทึกลง index ถาวร ไม่ปนกับนโยบายจริง) และติดป้ายกำกับชัดเจนทั้งใน chat memory และ UI ว่าเป็นเนื้อหา AI สร้างที่ยังไม่ผ่านการอนุมัติ (ดู ADR-004) implement ผ่าน `_inject_draft_into_session()` ที่ฉีด synthetic user+assistant turn เข้า `ChatMemoryBuffer` ของ session เดียวกับที่ `/chat` ใช้ — ไม่ได้แก้กฎเหล็กของ `_build_sys_prompt()` แต่อย่างใด

## เอกสารที่เกี่ยวข้อง

- [README.md](README.md) — วิธีติดตั้ง/ใช้งาน และสถาปัตยกรรมระบบ
- [ADR.md](ADR.md) — บันทึกการตัดสินใจเชิงสถาปัตยกรรม
- [handoff.md](handoff.md) — สรุปสถานะโปรเจกต์สำหรับส่งต่องาน
